/**
 *
 * 需要持久化的数据
 *
 */

const piniaStorageLocal = {
    'pure-app': ['navTags', 'settings'],
}
const piniaStorageSession = {
    // 'pure-app': ['navTags', 'settings'],
}

export { piniaStorageLocal, piniaStorageSession }
