export type positionType = {
    startIndex?: number
    length?: number
}

export type setType = {
    title: string
    fixedHeader: boolean
    hiddenSideBar: boolean
}

export type userType = {
    token: string
    name?: string
}
